{
  "name": "@repo/user-service",
  "version": "0.0.1",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo dev user-service",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest"
  }
}
