{
  "name": "@repo/merchant-app",
  "version": "0.0.1",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo dev merchant-app",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest"
  }
}
